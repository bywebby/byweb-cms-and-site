{{--считает количество элементов--}}
@if($page['num'] == 1 || $page['num'] == null)
    {{ ++$k }}
@else
    {{ (++$k)+($page['num']-1)*$page['step'] }}
@endif
{{--конец подсчета количество элементов--}}
